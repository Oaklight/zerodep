# Skills 性能测试

本模块不提供性能测试。

!!! info "为什么没有性能测试？"
    `skills` 模块实现了 [Agent Skills](https://agentskills.io) 规范 -- 解析 `SKILL.md` 文件、管理注册表和生成系统提示词。它是一个**没有直接第三方对标库**的工具模块。

    官方 `skills-ref` 包是一个极简的参考实现（仅 3 个函数），明确标注为"非生产用途"。技能解析是低频操作（启动时执行一次），因此性能比较没有实际意义。
