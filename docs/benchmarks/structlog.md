# Structured Logging Benchmark

Benchmarked against `structlog` across four scenarios (console rendering, JSON rendering, key-value rendering, bound logger context propagation).

Run `make benchmark-structlog` to reproduce.
