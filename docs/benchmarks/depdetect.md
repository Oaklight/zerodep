# Depdetect 性能测试

本模块不提供性能测试。

!!! info "为什么没有性能测试？"
    `depdetect` 模块是一个**开发工具**，用于从 Python 源代码、requirements 文件和自由文本兼容性字符串中检测和验证外部依赖。它没有直接对应的第三方库可供性能对比。

    依赖检测通常是低频操作（在启动时或 CI 中运行一次），因此性能比较没有实际意义。
