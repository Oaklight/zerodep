# YAML Benchmark

Benchmarked against `PyYAML` across three input sizes (small, medium, large) for both load and dump operations.

Run `make benchmark-yaml` to reproduce.

*Results will be added after benchmark execution.*
